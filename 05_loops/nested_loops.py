# Nested Loop - It's a concept in which we can put one loop inside another loop.

# Example:

for x in range(5):              # Outer-Loop
    for y in range(3):          # Inner-Loop
        # print({x}, {y})
        print(f"({x}, {y})")
